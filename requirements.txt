# Phase 1 - Risk Monitor
requests==2.32.3        # every HTTP call: GDELT, PEGELONLINE, and all three LLM providers
feedparser==6.0.11      # RSS feeds (the multilingual earliness edge)
python-dotenv==1.0.1    # reads .env

# Phase 4 - web app (installed now so setup happens once)
fastapi==0.115.6
uvicorn==0.34.0
